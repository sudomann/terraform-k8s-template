/* The following code is intended to be in a file called terraform.tfvars which you don't check into source control
   Terraform docs also describe alternate methods of providing these credentials, e.g. command line arguments,
   env vars, etc.
*/ 
gcp_credentials_production = "/absolute/path/to/service-account.json"

gcp_credentials_development = "/absolute/path/to/service-account.json"
